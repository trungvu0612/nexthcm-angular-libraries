import { JobTitlesPipe } from './job-titles.pipe';

describe('JobTitlesPipe', () => {
  it('create an instance', () => {
    const pipe = new JobTitlesPipe();
    expect(pipe).toBeTruthy();
  });
});
