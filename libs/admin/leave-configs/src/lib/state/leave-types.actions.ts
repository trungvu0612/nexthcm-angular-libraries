import { createAction } from '@datorama/akita-ng-effects';

export const loadLeaveTypes = createAction('[Leave types] Load leave types');
export const refreshLeaveTypes = createAction('[Leave types] Refresh leave types');
