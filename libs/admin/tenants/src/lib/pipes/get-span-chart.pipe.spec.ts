import { GetSpanChartPipe } from './get-span-chart.pipe';

describe('GetSpanChartPipe', () => {
  it('create an instance', () => {
    const pipe = new GetSpanChartPipe();
    expect(pipe).toBeTruthy();
  });
});
