import { ChartNodeHoverDirective } from './chart-node-hover.directive';

describe('ChartNodeHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ChartNodeHoverDirective();
    expect(directive).toBeTruthy();
  });
});
