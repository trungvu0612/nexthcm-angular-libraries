import { SeatUserStatePipe } from './seat-user-state.pipe';

describe('SeatUserStatePipe', () => {
  it('create an instance', () => {
    const pipe = new SeatUserStatePipe();
    expect(pipe).toBeTruthy();
  });
});
