import { TransferLeaveEntitlementTypePipe } from './transfer-leave-entitlement-type.pipe';

describe('TransferLeaveEntitlementTypePipe', () => {
  it('create an instance', () => {
    const pipe = new TransferLeaveEntitlementTypePipe();
    expect(pipe).toBeTruthy();
  });
});
