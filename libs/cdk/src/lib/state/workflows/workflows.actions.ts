import { createAction } from '@datorama/akita-ng-effects';

export const loadWorkflows = createAction('[Workflows] Load Workflows');
export const refreshWorkflows = createAction('[Workflows] Refresh Workflows')
