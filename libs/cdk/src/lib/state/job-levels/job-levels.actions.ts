import { createAction } from '@datorama/akita-ng-effects';

export const loadJobLevels = createAction('[Job levels] Load Job levels');
export const refreshJobLevels = createAction('[Job levels] Refresh Job levels');
