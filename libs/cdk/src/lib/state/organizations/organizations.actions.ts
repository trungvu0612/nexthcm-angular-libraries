import { createAction } from '@datorama/akita-ng-effects';

export const loadOrganizations = createAction('[Organizations] Load organizations');
export const refreshOrganizations = createAction('[Organizations] Refresh organizations');
