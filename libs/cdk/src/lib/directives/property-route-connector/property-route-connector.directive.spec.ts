import { PropertyRouteConnectorDirective } from './property-route-connector.directive';

describe('PropertyRouteConnectorDirective', () => {
  it('should create an instance', () => {
    const directive = new PropertyRouteConnectorDirective();
    expect(directive).toBeTruthy();
  });
});
