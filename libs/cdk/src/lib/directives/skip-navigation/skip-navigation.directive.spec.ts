import { SkipNavigationDirective } from './skip-navigation.directive';

describe('SkipNavigationDirective', () => {
  it('should create an instance', () => {
    const directive = new SkipNavigationDirective();
    expect(directive).toBeTruthy();
  });
});
